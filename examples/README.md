# Examples

Experimental scripts for exploring `open-foi` capabilities. These are **not** shipped as part of the package.

## Usage

Scripts assume an editable install of the package:

```bash
cd /path/to/my-society-cli
uv venv && uv pip install -e ".[dev,browser]"
python examples/<script>.py
```
